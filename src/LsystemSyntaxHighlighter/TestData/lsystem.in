
let globalVar = e ^ pi;

fun flobalFun(some, params) {
	let localVar = some - params;
	localVar * localVar
}

/**
 * Doc comment
 *
 * This is Test
 */
lsystem TestLsystem(lsys, params = {0,1,2}) {

	fun badGuy(a, a) { a }
	fun badGuy'(a = 5, b) { NaN }

	// function definition
	fun f(x, y, z = log(e)) {
		let xz = x * z;
		let yz = y * z;
		sqrt(x + 2 * xz + yz * xy)  // return value
	}

	// this is variable
	let a = 10 * f(1.5);

	//* Set keyword allows to define symols
	set axiom = abcd(e)f;

	// rewrite rules
	rewrite { lct(x)t } a(pa,ra,_,ms) { r(c)txt }
		with a = x+c, b = a*a*a
		where a * a > a + a
		to r(e + 1)p lac
		weight pi * e;

	rewrite{x(a)}y(a){z(a)}to;

	rewrite {x(_, _, x1)} y(a) {z(_, z2)} to nothing;

	rewrite i(ntru,der)
		with error
		where (1+2
		to nothing;
}

/*/

lsystem AnabaenaCatenula {
	rewrite A to AB;
	rewrite B to A;
}

lsystem OneLiner { } lsystem AnotherOneLiner { }

lsystem SyntaxError {
	let x = 0;
	let a = 0
	let b = 0;
	let = 0;
	let {a} = 0;
	let g = ;
	let let = let;
	let=0;
//	a = 0;
	; ;;
}

/*
nested
/*
multi-line
*/
comments
*/

let notComment = {};

/* unterminated comment




