@using System.Web.Mvc

@helper Js(string fileName, UrlHelper url) {
	<script src="@url.Content("~/Scripts/" + fileName)"></script>
}

@helper Css(string fileName, UrlHelper url) {
	<link rel="stylesheet" href="@url.Content("~/Content/" + fileName)" />
}